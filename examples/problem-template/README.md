# Cart discount bug

A small Node service where the discount code is applied to shipping as well as items. Used as the template problem repository; copy this folder to a new repo to author a problem. See `docs/PROBLEM-FORMAT.md` in the platform repo.
