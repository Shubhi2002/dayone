# @dayone/core

Domain model: branded ids, entities, the session state machine, trace event schema, domain errors. **No I/O, no imports from other packages.** See `docs/DOMAIN.md` and `docs/EVENTS.md`.
