# @dayone/infrastructure

Adapters for the non-provider ports: repositories, trace/snapshot/blob stores, queue, clock, ids, tokens, mailer, logger. In-memory implementations for development and tests; Postgres/Redis/S3 implementations are stubs to fill in (`src/postgres`, `src/redis`).
