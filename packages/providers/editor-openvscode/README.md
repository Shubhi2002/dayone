# @dayone/editor-openvscode

`EditorProvider` for OpenVSCode Server running inside the sandbox. Expects the binary at `/opt/openvscode` (baked into the template). Starts with a connection token, marketplace and settings sync disabled, and installs extensions from VSIX or id.
